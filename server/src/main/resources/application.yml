spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nextchat
    username: root
    password: 1123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # or validate / create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  data:
    mongodb:
      uri: mongodb://localhost:27017/message_db
